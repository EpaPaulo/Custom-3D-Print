{
  "name": "lego-plate-shop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Order backend for custom LEGO-compatible baseplates. Builds the print file from the same geometry module the configurator previews.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node test/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
